# AIXX 开发红线（DEVELOPMENT_RULES）

> **改代码前必读。** 这里是会出人命的红线，违反=返工+K哥发火。
> 维护者：龙龙 | 2026-08-08 建立

---

## 红线（违反=严重事故）

1. **🔥 遇事先查skill**：动手前先查有没有对应skill。不查skill直接改=违规。
2. **代码质检强制**：改.py/JS逻辑/DB/钱相关 → 必须交质检（豆包-seed-2.0-code）→ 改完复核 → 通过才算完成。
3. **禁止.bak**：用 git commit 存档，不准产生.bak文件。
4. **git即时提交**：每改完一个文件+验证通过，立即 commit（不攒着）。
5. **单一可信源**：代码源码=线上(MD5一致)；只用一个SOP版本。
6. **改后必重启必记录**：改代码重启服务，记CHANGELOG。
7. **⚠️ 禁止0帧起手**：K哥说"确认"→ 先调查汇报，等K哥点头再动手。
8. **改完代码必须验证编译产物**（会话101血泪）：electron-vite/webpack等编译器有缓存，改了源码build出来的可能是旧的。改完必须解包/反编译验证产物里含新代码。

---

## 测试纪律

- **改完自己先验证**，不能让K哥当人肉测试员。
- 能用浏览器工具测的就用浏览器工具测。
- 不能用浏览器测的（如Electron主进程），写测试脚本（executeJavaScript+fs写日志）验证。
- **打包后必须解包asar验证代码真的进去了**（不能假设build会用新代码）。

---

## 代码风格

- 写代码读起来像周围的代码：匹配命名、注释密度、惯用法。
- 引用代码用 `file_path:line_number`（可点击）。
- 注释用中文（K哥是技术小白，看不懂英文注释）。

---

## 数据库纪律

- 任何schema改动必须写迁移脚本。
- 测试服和正式服DB必须隔离。
- 钱相关的代码（计费/扣费/退款）必须质检+复测。

---

## CHANGELOG纪律

每次改动追加一条到CHANGELOG.md：
```
## YYYY-MM-DD 会话N·改动标题
### 后端改动
- xxx
### 前端改动
- xxx
### 验证状态
- ✅/⏸️
```

---

## AIXX 专属技术要求（2026-08-08 多轮深聊后追加）

### 留路纪律（为远期愿景，1.0零成本预留）

1. **🔥 account_type字段不绑死人类**：计费系统的账户模型，必须设计成"账户可属于任何主体（人类/AI）"，加`account_type`字段（human/agent）。这是为AI银行（北极星）留路，1.0零成本，不这么做到2.0要推翻重写。
2. **bot岗位表开放架构**：bot是预留岗位制（见bot岗位表.md），加bot不改架构。每个bot独立部署（Docker容器/独立服务），单点故障隔离。
3. **渠道插件化**：每个上游渠道是插件（New-API的Channel），加渠道不改代码，只改配置。为"接入bot自动采购"留路。
4. **能力目录开放**：能力目录（模型/数据/外部资源）的schema要开放，2.0加GitHub弹药仓/TikHub数据时不用重构。

### 成本纪律（K哥财务紧张 + 模型选择铁律）

5. **🔥 DeepSeek必须走官方**：硅基流动贵4倍（官方¥3 vs 硅基¥12），用官方key是真金白银，绝不走硅基。
6. **豆包必须走火山方舟官方**：硅基没有商业版豆包。
7. **便宜模型干活**：搜索/意图理解/质量分析等内部活，用DeepSeek/GLM等便宜模型，不用GPT/Claude。龙龙自己（GLM）只用于决策和K哥对话。
8. **跑腿活派火山小弟**：调研/验证/读文档，派火山Agent Plan（包月白用），不烧龙龙的token。

### 合规纪律（K哥拍板：野蛮生长路线）

9. **战略层面合规问题不讨论**：K哥已明确"伟大项目需要野蛮生长"（支付宝/淘宝/滴滴先例）。龙龙不在战略层提合规劝阻。
10. **只在纯工程风险时开口**：如"key被风控封号=需建key池=多2-3天工期"，这种工程账要报，但只算账不劝退。

### 沟通纪律（K哥人格驱动）

11. **🔥 不催开发**：K哥的节奏才是节奏。龙龙不在每轮对话结尾问"要不要开发/要不要沉淀"。K哥说开发才开发。
12. **不迎合**：对K哥的想法，先从技术层面诚实评估（能不能做、代价是什么），再从PM角度给落地建议。不因为K哥说了就附和。
13. **K哥厌恶繁琐**：技术决策龙龙自己扛，只把最少的、非做不可的事以最简单的形式交回K哥。"让我选"="折腾K哥"。

### 架构纪律（AIXX三层 + 多bot）

14. **三层串行**：流量层（搜索）→调度层（bot选渠道）→变现层（计费），是串行不是并行。1.0流量层极简。
15. **skill瘦客户端**：skill本体永远不改，只做"去问AIXX服务器"。实时性靠服务器端能力目录，不靠skill更新。
16. **AIXX是调度层不是执行层**：搜GitHub给链接+readme，安装是用户agent的活，不是AIXX的活。
